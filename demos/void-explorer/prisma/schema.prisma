// Generated by Void Manifest Engine
// DO NOT EDIT BY HAND — regenerate from the Architectural Graph

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Sutra {
  id Int @id @default(autoincrement())
  title String
  sanskritText String
  englishMeaning String
  category String
  createdAt DateTime
  updatedAt DateTime
}

model SutraRelation {
  id Int @id @default(autoincrement())
  sutraId Int
  relatedSutraId Int
  Sutra Sutra @relation(fields: [sutraId], references: [id])
  Sutra Sutra @relation(fields: [relatedSutraId], references: [id])
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
